/**
 * ==================================================
 * FEATURED CATEGORY — UI TYPE
 * ==================================================
 *
 * ✅ NORMALIZED
 * ✅ SAFE FOR COMPONENTS
 * ❌ NOT RAW API
 */

export type FeaturedCategory = {
  id: number;
  name: string;
  slug: string;
  image: string | null;
};
